<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>System Information</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
        }
        h1 {
            color: #333;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }
        th, td {
            padding: 10px;
            border: 1px solid #ccc;
            text-align: left;
        }
        th {
            background-color: #f4f4f4;
        }
        .no-changes {
            background-color: #d4edda; /* light green */
        }
    </style>
</head>
<body>

{% for host in ansible_play_hosts_all %}

<h1>System Information Report</h1>

<h2>Host Information</h2>
    <p><strong>Hostname:</strong> {{ host }} </p>

    {% set hm_precheck = host + '_precheck_facts_result' %}
    {% set hm_precheck_vars = lookup('vars', hm_precheck) %}

<p><strong>Hostname </strong>: {{ host }} </p>
<p><strong>IP Address </strong>: {{ hm_precheck_vars['ansible_facts']['ansible_enp1s0']['ipv4']['address'] }}</p>

<h3>Precheck vs. Postcheck Comparison</h3>
<table>
<tr> 
    <th> Attributes </th>
    <th> Precheck </th>
    <th> Postcheck </th>
    <th> Changes </th>
</tr>
    <td> Distribution </td>
    
    <tr>
    {% set pre_distribution = host + '_pre_distribution' %}
    {% set post_distribution = host + '_post_distrbution %}
    {% set pre_distribution_vars = lookup('vars', pre_distribution) %}
    {% set post_distribution_vars = lookup('vars', post_distribution) %}
    
    <td>  {{ pre_distribution_vars }} </td>
    <td>  {{ post_distribution_vars }} </td>
    </tr>
</table>


{% endfor %}
</body>
</html>
