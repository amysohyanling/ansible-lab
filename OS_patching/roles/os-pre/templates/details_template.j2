<!DOCTYPE html>
<html>
<head>
    <title>Cluster System Information</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
        }
        h1 {
            color: #333;
        }
        .node {
            margin-bottom: 20px;
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 5px;
        }
        .node h2 {
            margin-top: 0;
        }
        .info {
            margin-bottom: 10px;
        }
        .info label {
            font-weight: bold;
            display: inline-block;
            width: 150px;
        }
    </style>
</head>
<body>
    <h1>Cluster System Information</h1>
    {% for node in nodes %}
    <div class="node">
        <h2>{{ node.hostname }}</h2>
        <div class="info">
            <label>System Information:</label>
            <span>{{ node.system_info }}</span>
        </div>
        <div class="info">
            <label>OS Version:</label>
            <span>{{ node.os_family }} {{ node.distribution_version }}</span>
        </div>
        <div class="info">
            <label>IP Address:</label>
            <span>{{ node.ip_address }}</span>
        </div>
    </div>
    {% endfor %}
</body>
</html>